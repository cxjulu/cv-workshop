import type { ReactiveController, ReactiveControllerHost } from 'lit';
export declare class InputModeDetector implements ReactiveController {
    private host;
    private abortMouseListener;
    private abortKeydownListener;
    /** We start by assuming keyboard is main input mode */
    inputMode: 'mouse' | 'keyboard';
    constructor(host: ReactiveControllerHost);
    hostConnected(): void;
    hostDisconnected(): void;
    private setMouseInputMode;
    private setKeyboardInputMode;
}
