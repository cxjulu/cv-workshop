import type { ReactiveController, ReactiveControllerHost } from 'lit';
type Breakpoint = 'mobile' | 'tablet' | 'desktop';
export declare class BreakpointObserver implements ReactiveController {
    private host;
    private query;
    private abortController;
    matches: boolean;
    constructor(host: ReactiveControllerHost, query: Breakpoint);
    private setNewValue;
    hostConnected(): void;
    hostDisconnected(): void;
}
export {};
