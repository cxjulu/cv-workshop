import type { ReactiveController, ReactiveControllerHost } from 'lit';
type Options = {
    signal: AbortSignal;
};
/**
 * Gives support for swiping an element off the screen.
 * To start the swipe listener, call the `startSwipeAwayListener`
 * and provide the element you wish to observe. If you want a specific
 * element within the element to function as the drag handle, decorate
 * it with a `[data-drag-handle]` attribute.
 */
export declare class SwipeAway implements ReactiveController {
    private host;
    private draggedElement;
    private abortController;
    private startYPos;
    private prevEvent;
    private velocity;
    private callbackFn;
    constructor(host: ReactiveControllerHost);
    hostDisconnected(): void;
    startSwipeAwayListener(el: HTMLElement, callbackFn: typeof this.callbackFn, opts?: Partial<Options>): void;
    private onMouseMove;
    private onMouseUp;
}
export {};
